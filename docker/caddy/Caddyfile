# Evolution API em subdomínio dedicado
evolution.64.181.189.97.nip.io {
    reverse_proxy evolution-api:8080
}

# Domínio principal do sistema
{$DOMAIN} {
    # API
    handle /api/* {
        reverse_proxy backend:8000
    }

    # Health check
    handle /health {
        reverse_proxy backend:8000
    }

    # Frontend
    handle {
        reverse_proxy frontend:3000
    }
}
